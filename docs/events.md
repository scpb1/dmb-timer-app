# События и праздники

Документация вкладки «События»: служебные события, календарные праздники и их связь с темой письма в дневнике.

## Что использовано

Проект на **Expo SDK 51** (см. `package.json`). AGENTS.md ссылается на docs Expo 57, но код пишется под установленный SDK.

| Задача | API / библиотека |
| --- | --- |
| Хранение списков | `config` в `expo-sqlite` (`EVENTS_DATA`, `HOLIDAYS_DATA` – JSON) |
| UUID пользовательских записей | `expo-crypto` (`randomUUID`) |
| Даты | `date-fns` + `@react-native-community/datetimepicker` |
| Навигация | вкладка `Events` в `MainTabNavigator` |

Новые npm-зависимости не добавлялись.

## Два типа карточек в одном списке

```
Вкладка Events
 └─ EventsScreen
     ├─ список: события + праздники, сортировка по дате
     ├─ «+» → AddKindMenu (событие / праздник)
     ├─ EventFormModal      ддд или календарная дата
     └─ HolidayFormModal    только календарная дата (день+месяц)
```

События – вехи службы (призыв, месяцы, ддд/дпп, 1/4 и 1/3 службы, экватор, светофор, дембель и пользовательские). У каждой карточки минималистичная иконка: месяцы — `1/12`, Новый год — ёлка, 8 июля — кольцо и т.д.  
Праздники – даты внутри срока службы. Один шаблон (день+месяц) разворачивается в отдельную карточку на каждый год от призыва до дембеля: «Новый год 2027», «Новый год 2028» и т.д. Карточка другого цвета. В день праздника письмо девушки солдату открывается с темой «Как я отмечала {название} {год}».

## События

Хранение: `config.events_data`.

```ts
{ deletedIds, overrides, customEvents }
```

Предустановленные шаблоны считаются из дат призыва и дембеля (`src/utils/eventCalculations.ts`). Удаление пресета – id в `deletedIds`. Правка пресета – запись в `overrides`. Свои события – `customEvents`.

Автоматические вехи доли службы (кроме экватора):

| Событие | Как считается |
| --- | --- |
| Прошла 1/4 службы | четверть срока после призыва |
| Осталась 1/4 службы | четверть срока до дембеля |
| Прошла 1/3 службы | треть срока после призыва |
| Осталась 1/3 службы | треть срока до дембеля |

На карточке слева иконка: месяцы — `1/12`…`12/12`, доли — `1/4` и `1/3`, праздники — ёлка, звезда, кольцо и т.д. (`src/utils/eventGlyphs.ts`).

Форматы даты события:

- `ddd` – дней до дембеля
- `dpp` – только у части пресетов (в форме сводится к дате)
- `date` – конкретный календарный день

Без дат призыва/дембеля список событий пуст. Праздники тогда показываются одной карточкой на текущий календарный год.

## Праздники

Хранение: `config.holidays_data`. Та же схема, что у событий:

```ts
{ deletedIds, overrides, customHolidays: [{ id, name, month, day }] }
```

`month`/`day` – 1-based. Год в форме не хранится: при показе шаблон разворачивается на все годы службы. 29 февраля в невисокосный год показывается и срабатывает как 28 февраля.

### Предустановленные

| Дата | Название |
| --- | --- |
| 1 января | Новый год |
| 7 января | Рождество |
| 14 января | Старый Новый год |
| 19 января | Крещение |
| 14 февраля | День святого Валентина |
| 23 февраля | 23 февраля |
| 8 марта | 8 марта |
| 9 мая | День Победы |
| 8 июля | День семьи, любви и верности |
| 1 сентября | 1 сентября |

Пользовательские – кнопка «+» → «Праздник»: название и дата. Годовщина, дни рождения и любые свои дни добавляются так.

Изменение и удаление как у событий: карандаш / крестик, снекбар «Отменить удаление» 5 с. Пресет при удалении скрывается через `deletedIds` и возвращается после сброса данных. Свой праздник удаляется из массива.

Цвет карточки: `colors.light.holiday` (пыльная коралловая заливка и рамка), события остаются бежево-белыми.

## Тема письма в день праздника

Каталог из 2000+ тем (`letter_themes`) по-прежнему случайный. Исключение – день, у которого есть праздник.

1. `useLetterThemes(date)` подмешивает виртуальные темы `holiday:{holidayId}:{year}`.
2. Заголовок: `Как я отмечала {название} {год}` (год берётся из даты письма, не из формы праздника).
3. Первый показ экрана темы выбирает праздничную тему (`pickPreferredTheme`). Shuffle может взять другую свободную тему из каталога; праздничную можно вытянуть снова, пока письмо за этот день не сохранено.
4. Если на дату несколько праздников, первыми идут пользовательские, затем пресеты.
5. Виртуальные id в таблицу `letter_themes` не пишутся. После сохранения `theme_id` лежит в `diary_entries`, как у обычных тем.
6. Если праздник потом удалили, заголовок старого письма восстанавливается из id или становится «Как я отмечала этот день {год}».

`resetAllData` чистит `config` целиком: пользовательские праздники пропадают, пресеты праздников снова на месте.

## Потоки экранов

### Добавить праздник

Вкладка События → «+» → Праздник → название + дата → Готово. В списке появляются коралловые карточки на каждый год службы, например «Новый год 2027». Под датой: «Осталось n дней до даты». Прошедшие карточки полупрозрачные.

### Письмо 1 января

Дневник → письма → если на 1 января ещё нет записи → «Твоя тема на сегодня:» «Как я отмечала Новый год 2026» (год как у выбранной даты).

## Решения, которые сознательно приняты

1. **JSON в `config`, не отдельная SQL-таблица.** Совпадает с событиями; пресеты можно пересчитать без миграции столбцов.
2. **Только день и месяц в хранении.** Иначе Новый год 2025 и 2026 пришлось бы заводить дважды. На экране шаблон разворачивается на годы службы; год в теме письма берётся из даты дневника.
3. **Список праздников зависит от дат службы.** Без призыва/дембеля остаётся одна карточка на текущий календарный год.
4. **Праздничная тема не вытесняет каталог насовсем.** В этот день она первая, но shuffle остаётся.
5. **Под датой нет ддд/дпп.** И у событий, и у праздников подпись «Осталось n дней до даты» (для сегодня — «Сегодня»).

## Файлы

- `src/types/events.ts` – типы событий
- `src/types/holidays.ts` – типы и сиды праздников
- `src/utils/eventCalculations.ts` – пресеты службы, список событий
- `src/utils/eventGlyphs.ts` – иконки карточек
- `src/utils/holidayCalculations.ts` – даты, совпадение с днём, виртуальные темы писем
- `src/hooks/useEvents.ts` / `src/hooks/useHolidays.ts` – CRUD и undo
- `src/hooks/useDiary.ts` – подмешивание праздничной темы
- `src/screens/events/EventsScreen.tsx` – общий список
- `src/components/events/*` – карточка, формы, меню «+», снекбар
- `src/theme/colors.ts` – `light.holiday`
- `docs/events.md` – этот файл
- `docs/diary.md` – дневник и отсылка сюда
